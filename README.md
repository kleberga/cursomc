# Projeto de backend usando Java com Spring Boot e JPA

- O projeto consistiu em criar um backend usando Java com Spring Boot e JPA.
- Foram criadas classes de clientes, produtos, categorias, endereco, pedido, entre outras.
- Algumas instâncias de cada classe foram salvas em memória usando o banco de dados H2.
